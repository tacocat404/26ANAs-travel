# 프로젝트 작업 규칙 (Claude용)

<!--
  ★ 사용법 ★
  1. 이 파일을 새 프로젝트 폴더의 맨 위(루트)에 복사한다. 파일 이름은 반드시 CLAUDE.md
  2. 아래 [대괄호] 부분을 그 프로젝트에 맞게 고친다. (안 고쳐도 로그 규칙은 작동함)
  3. 끝. 그 폴더에서 Claude와 대화하면 이 규칙을 자동으로 읽고 따른다.
-->

## 프로젝트 소개
- 프로젝트 이름: 언제갈까? (여행 일정 캘린더)
- 목적: 친구들이 각자 "안 되는 날"을 색깔로 표시해 모두 갈 수 있는 여행 날짜를 찾는 웹앱
- 저장소/배포: 아직 없음 (예정: GitHub tacocat404 + Vercel — 절차는 `docs/SETUP.md`)

## 작업 로그 (필수)
- **모든 작업(코딩/기획/배포/콘텐츠)이 끝나면 반드시 `docs/DEVLOG.md`에 항목을 추가한다.**
  파일이 없으면 처음 작업할 때 만든다.
- 형식: 날짜 섹션(`## YYYY-MM-DD`) 아래 `### [태그] 제목`
  - 태그는 `[기획]` / `[구현]` / `[배포]` / `[콘텐츠]` / `[자동화]` 중 선택.
- 각 항목에 쓸 것: **사용자 프롬프트(요약)** + **Claude의 변경사항** + (있으면) **다음 단계**.
- 사용자가 따로 말하지 않아도 자동으로 기록한다.

## 운영 방식
- 운영자는 **비개발자** — 코드는 Claude 전담. 설명은 전문용어 없이 쉽게.
- 계정 로그인·프로그램 설치는 사용자가 직접 한다. Claude는 push/배포 같은 기계적 단계만 수행.
- 되돌리기 어려운 작업(삭제, 외부 공개 등)은 실행 전에 먼저 물어본다.

## 기술 주의사항
- ⚠️ **PowerShell로 한글이 든 파일을 텍스트 치환하지 말 것** (인코딩 깨짐) — Edit/Write 도구 사용.
- 기술 스택: React + Vite. 화면 코드는 `src/*.jsx`, 스타일은 `src/styles.css` 한 파일.
- 저장소는 2단 구조(`src/store.js`): `src/config.js`에 Supabase 키가 비어 있으면 데모 모드(localStorage), 채워지면 공유 모드. 두 모드는 같은 함수·같은 데이터 모양(snake_case)을 쓴다.
- DB 스키마는 `supabase/schema.sql` — 테이블 바꾸면 이 파일도 같이 갱신할 것.
- "안 되는 날"(unavailable)은 여행별이 아니라 **사람 기준 전역** 저장 (설계 이유는 `docs/PLAN.md` §4).
